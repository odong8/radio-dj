# Visual verification notes

- 2026-08-22: 데스크톱 1280px 전체 화면에서 상단 BGM 토글, 2열 LIVE STUDIO·ON AIR 구성, 4개 DJ 카드와 보관함 빈 상태가 정상적으로 표시됨을 확인했다.
- 2026-08-22: 모바일 375px 전체 화면에서 상단 BGM 토글이 유지되고, 입력 폼·ON AIR·보관함이 단일 열로 자연스럽게 쌓이며 텍스트와 버튼이 잘리지 않음을 확인했다.
- 2026-08-22: 데스크톱에서 네 개의 DJ 카드마다 보이스명, 성별·연령·음역 정보, 미리듣기 및 별표 제어가 표시되고, 즐겨찾기 빈 상태 영역이 확인되었다.
- 2026-08-22: 모바일 375px에서 보이스 프로필 카드와 미리듣기 버튼이 충분한 높이로 단일 열 배치되며, ON AIR·즐겨찾기·보관함까지 스크롤 흐름이 유지되었다.
- 2026-08-22: 데스크톱 전체 화면에서 과몰입 새벽은 Liam·남성 20대·밝고 에너지 있는 음색, 차분한 응원은 Chris·남성 30대·부드럽고 안정적인 중저음으로 표시되고 각 미리듣기 제어가 유지됨을 확인했다.
- 2026-08-22: 데스크톱에서 감정 표현·말투 강도 1~5 슬라이더, 현재 레이블, 단계별 보조 레이블이 DJ 카드 앞에 표시되어 연출 값을 명확히 확인할 수 있다.
- 2026-08-22: 모바일 375px에서도 두 슬라이더의 손잡이와 단계 라벨이 충분히 분리되어 표시되며, DJ 카드·사연 입력·ON AIR 흐름이 단일 열로 유지된다.
- 2026-08-22: 데스크톱에서 감정 표현·말투 강도 슬라이더의 현재 레이블과 1~5단계 범위가 명확히 보이며, DJ 선택 및 방송 시작 제어와 간섭하지 않는다.
- 2026-08-22: 모바일 375px에서 강도 제어 카드가 DJ 카드보다 앞에 유지되고, 단계 라벨·손잡이·방송 시작 버튼이 모두 한 화면 흐름에서 읽을 수 있다.
- 2026-08-22: ON AIR 카드의 데스크톱 높이 보정 선택자가 토스트 알림의 aria-live 영역까지 적용되어 상단 공백이 생긴 것을 확인했다. 선택자를 main 하위 ON AIR 카드로 한정해 수정했다.
- 2026-08-22: 데스크톱에서 4단계 입력 폼과 ON AIR 카드의 폭을 0.96:1.04로 보정하고, 결과 카드에만 데스크톱 최소 높이를 적용해 시각 균형을 맞췄다.
- 2026-08-22: 모바일 375px에서 강도 카드가 제거된 1·기분, 2·에너지, 3·DJ, 4·사연 흐름이 한 열로 유지되고 ON AIR·즐겨찾기·보관함이 읽기 쉬운 간격으로 이어짐을 확인했다.
- 2026-08-22: 데스크톱에서 즐겨찾기·보관함 섹션은 2rem 패딩과 1rem 카드 간격을 적용해 상단 방송 카드와 균형을 이루며, 보관함은 3열 카드 구조를 유지한다.
- 2026-08-22: 모바일 375px에서 즐겨찾기·보관함 섹션은 1.5rem 라운드와 1.5rem 상단 여백으로 구분되고, 보관함 카드는 단일 열에서 0.75rem 간격을 유지한다.
- 2026-08-22: 데스크톱 1280px에서 오류가 보고된 `/?from_webdev=1` 메인 화면과 모든 입력 제어가 정상 로드됨을 확인했다. 현재 검증 브라우저에는 앱 로그인 세션이 없어 방송 시작 시 OAuth 로그인으로 전환되었으며, 로그인 후 자동 음성 생성으로 이어지는 전체 UI 흐름은 실제 계정 세션에서 별도 확인이 필요하다.
- 2026-08-22: 서버 tRPC `radio.generate`는 실제 요청에서 title·opening·body·closing을 모두 포함한 JSON으로 성공 응답했다. nano의 누락 필드 응답은 mini 재시도로 복구되는 단위 테스트를 추가했으며, 향후 재발 시 모델·finish reason·응답 형식·스키마 오류 경로만 기록하도록 했다.
- 2026-08-22: 재시작 후 실제 `radio.generate` 응답에서 `body: "{"`처럼 JSON 구조는 맞지만 방송 문장으로는 불완전한 nano 출력이 확인됐다. 각 필드에 최소 길이와 읽을 수 있는 문자 수를 적용해 이를 스키마 실패로 판정하고 mini로 재시도하도록 보강했다. 이후 동일 입력으로 실제 요청한 결과는 네 문단 모두 완전한 문장으로 HTTP 200 응답했다.
