# Project TODO

- [x] 심야 라디오 제품 경험, 입력 데이터, 결과 JSON 스키마와 LLM 프롬프트를 정의한다.
- [x] 서버 측 tRPC 프로시저에서 구조화된 AI DJ 방송 대사를 생성하고 입력값을 검증한다.
- [x] LIVE STUDIO 입력 폼에 5종 기분, 1~5 에너지, 지정된 4종 DJ 모드, 120자 사연 입력을 구현한다.
- [x] 심야 라디오 감성의 ON AIR 결과 카드와 CSS 파형 애니메이션을 구현한다.
- [x] 같은 사연을 다른 DJ 모드로 재생성하는 흐름을 구현한다.
- [x] LocalStorage 기반 지난 방송 보관함을 최대 20개·최신순·상세 모달·개별 삭제로 구현한다.
- [x] 빈 상태, 입력 오류, AI 생성 중·실패 상태를 명확하게 처리한다.
- [x] 1280px 2열 및 375px 단일 열 반응형 레이아웃을 구현한다.
- [x] LLM 프롬프트·입력 검증의 Vitest 테스트를 작성하고 타입 검사·빌드를 통과시킨다.
- [x] 데스크톱과 모바일 화면을 시각적으로 검증한다.
- [x] ON AIR 결과를 사용자가 직접 재생·정지할 수 있는 브라우저 음성 방송 기능을 추가한다.
- [x] 음성 기능을 지원하지 않는 환경의 안내와 재생 상태 표시를 구현한다.
- [x] 서버 측 DJ 대사 생성을 경량 모델·최소 추론·간결한 원고 규칙으로 전환해 대기 시간을 줄인다.
- [x] 음성 재생과 빠른 생성 경로를 타입 검사·테스트·화면 검증으로 확인한다.
- [x] 브라우저 기본 음성 재생을 제거하고 서버 기반 AI 음성 생성 방식으로 교체한다.
- [x] 4종 DJ 모드에 맞는 자연스러운 심야 라디오 음성 프롬프트와 안전한 서버 처리 흐름을 설계한다.
- [x] 생성된 AI 오디오를 재생·로딩·실패 상태와 함께 ON AIR 카드에 연결한다.
- [x] AI 음성 생성·재생 흐름을 실제 오디오 결과와 화면 검증으로 확인한다.
- [x] ElevenLabs 무료 API 키 발급과 서버 측 보안 연동 절차를 안내한다.
- [x] 지정된 4개 DJ 모드에 서로 다른 자연스러운 ElevenLabs 보이스와 음성 설정을 매핑한다.
- [x] AI DJ 음성이 준비되면 한 번의 사용자 클릭으로 자동 재생되도록 재생 흐름을 간소화한다.
- [x] 기분별 심야 라디오 배경 음악을 생성·연결하고 DJ 대사보다 낮은 음량으로 반복 재생한다.
- [x] DJ 보이스·자동 재생·배경 음악의 데스크톱 및 모바일 오디오 경험을 검증한다.
- [x] 각 DJ 프로필에서 현재 음성을 짧게 미리 들을 수 있는 재생 제어를 추가한다.
- [x] 방송과 DJ 보이스를 LocalStorage 기반 즐겨찾기에 보관하고 다시 재생하는 기능을 추가한다.
- [x] 성별·연령·음역대가 다른 4개 DJ 보이스 프로필과 설명을 적용한다.
- [x] 미리듣기·즐겨찾기·다양한 보이스의 데스크톱 및 모바일 상호작용을 검증한다.
- [x] 과몰입 새벽을 20대 남성 보이스와 미리듣기 음원으로 교체한다.
- [x] 차분한 응원을 30대 남성 보이스와 미리듣기 음원으로 교체한다.
- [x] 변경된 DJ 프로필·미리듣기·서버 보이스 매핑을 검증한다.
- [x] 감정 표현과 말투 강도를 1~5단계로 조절하는 슬라이더 UI를 추가한다.
- [x] 두 강도값을 서버의 DJ 대본 프롬프트와 ElevenLabs 음성 연출 설정에 전달한다.
- [x] 선택한 강도를 ON AIR 결과와 즐겨찾기 재생에 일관되게 유지한다.
- [x] 강도 조절에 따른 대본·음성 생성 흐름과 반응형 UI를 검증한다.
- [x] ON AIR·상세 보기에서 선택한 감정 표현과 말투 강도를 표시한다.
- [x] 강도값이 포함된 서버 대본·음성 생성 요청을 통합 테스트로 검증한다.
- [x] 낮은·높은 강도 간 대본 지시문과 ElevenLabs 음성 설정 차이를 확대한다.
- [x] 강도 설정이 실제 방송 재생마다 새 음성 생성에 반영되도록 재생 흐름을 보강한다.
- [x] 현재 선택 강도의 효과를 사용자가 이해할 수 있도록 UI 피드백을 강화한다.
- [x] 극단 강도값의 대본·음성 파라미터 차이를 자동 테스트로 검증한다.
- [x] Google Cloud TTS 전환의 한국어 음성 품질·구현 복잡도·운영 차이를 현재 ElevenLabs 구현과 비교한다.
- [x] Qwen3-TTS의 라이선스·한국어 음성 품질·호스팅 요구사항과 현재 앱 전환 가능성을 조사한다.
- [x] ElevenLabs 크레딧 소모를 줄일 수 있는 캐시·재생 흐름 개선과 대체 TTS 전략을 비교한다.
- [x] DJ 연출 강도 슬라이더와 관련 서버 프롬프트·음성 설정·보관 데이터 필드를 제거한다.
- [x] 강도 변경에 따른 중복 TTS 재생성을 없애 단순한 방송 생성·재생 흐름으로 정리한다.
- [x] 데스크톱과 모바일에서 입력 스튜디오·ON AIR 카드·하단 보관함의 비율과 여백을 개선한다.
- [x] 간소화된 생성 흐름과 개선된 반응형 비율을 테스트·시각 검증한다.
- [x] 지난 방송 보관함의 패딩·카드 간격·열 수를 데스크톱과 모바일 비율에 맞춰 실제로 보정한다.
- [x] radio.generate 대사 생성 실패의 서버 로그·LLM 응답·입력값을 재현해 원인을 확인한다.
- [x] 구조화된 LLM 응답 처리와 사용자 오류 안내를 실패 원인에 맞게 보강한다.
- [ ] 수정된 대사 생성 경로를 실제 요청·테스트·브라우저 흐름으로 검증한다.
- [x] nano의 구조화 응답 실패 시 mini 복구 경로를 단위 또는 통합 테스트로 직접 검증한다.
- [ ] 로그인된 개인 브라우저에서 방송 생성과 ON AIR 표시를 검증한다.
- [x] 구조는 맞지만 본문이 단일 기호처럼 불완전한 AI 대사를 품질 실패로 판정하고 mini 재시도로 복구한다.
- [x] 현재 Manus 전용 인증·LLM·저장소 의존성을 Vercel 배포 관점에서 점검한다.
- [x] Vercel 환경 변수와 서버리스 API 구조를 설계하고 배포 설정을 추가한다.
- [x] GitHub 과제 제출용 README와 Vercel 배포 절차를 작성한다.
- [x] 외부 배포용 빌드·테스트를 실행하고 GitHub 저장소 준비 상태를 검증한다.
- [x] 과제 안내문·평가표의 제출 항목과 증빙 요건을 현재 산출물에 다시 매핑한다.
- [x] 평가 기준을 충족하는 PRD Markdown·PDF를 작성한다.
- [x] 제작 의도·아키텍처·기술 스택·프롬프트·PMI를 포함한 웹앱 설명서 Markdown·PDF를 작성한다.
- [x] Mermaid 아키텍처 원본과 PNG를 제출 문서에 포함한다.
- [x] 초보자용 GitHub 업로드·Vercel Git Import·환경 변수·배포 증빙 가이드를 작성한다.
- [x] node_modules·dist·로그를 제외한 제출용 소스 ZIP을 준비한다.
- [ ] 제출자 본인이 GitHub 저장소를 만들고 제출용 ZIP의 코드를 업로드한 뒤 저장소 URL을 PRD·설명서에 입력한다.
- [ ] 제출자 본인이 Vercel에서 GitHub 저장소를 Import하고 LLM·ElevenLabs 환경 변수를 등록한 뒤 배포 URL과 Git Import 증빙 캡처를 만든다.
- [x] 과제 평가 항목별 제출물·검증 증거·미완료 항목을 최종 점검표로 대조한다.
- [x] Vercel 방송 대사 생성기를 Gemini API 형식과 `GEMINI_API_KEY` 환경 변수로 전환한다.
- [x] Gemini 응답의 JSON·안전 필터·오류를 검증하고 서버리스 API 테스트를 갱신한다.
- [x] PRD·설명서·README·Vercel 가이드·최종 점검표를 Gemini 키 기준으로 갱신한다.
- [x] Gemini 전환 후 타입 검사·테스트·Vercel 빌드를 다시 실행하고 제출 ZIP을 갱신한다.
- [x] 제출용 ZIP은 정적 BGM을 유지하고, 프로젝트는 기존 파일 스토리지 BGM URL을 참조하도록 중복 대용량 MP3를 정리한다.
- [x] Vercel과 Manus 미리보기 모두에서 유효한 공개 BGM URL로 통일하고, 정리 뒤 Vercel 빌드·BGM 경로를 검증한다.
- [x] Vercel 도메인에서도 접근 가능한 BGM 공개 절대 URL을 확보하고 MP3 응답을 검증한다.
- [x] 배포된 Gemini 방송 대사 생성 오류의 환경 변수·모델·응답 원인을 확인한다.
- [x] Gemini API 실패 상태를 안전한 원인별 메시지와 진단 로그로 보강한다.
- [x] 수정 후 타입·테스트·Vercel 빌드를 실행하고 사용자가 재배포해 확인할 절차를 정리한다.
- [ ] 수정 코드를 GitHub에 올리고 Vercel을 재배포한 뒤 실제 사연 생성이 성공하는지 확인한다.
- [ ] Vercel 최신 배포가 수정된 Gemini 코드인지 확인하고 Runtime Logs의 Gemini HTTP 상태를 수집한다.
- [x] Vercel 함수가 공용 대사 생성 모듈을 항상 포함하도록 `api` 내부 라이브러리 구조로 이동한다.
- [ ] Vercel 배포 산출물에 공용 모듈이 포함되는지 확인하고 실제 `/api/generate` 500 오류를 재검증한다.
- [ ] Gemini 모델 404의 현재 지원 모델 ID를 공식 문서 기준으로 확인한다.
- [ ] 기본 Gemini 모델과 404 오류 안내를 지원되는 모델 ID로 교체한다.
- [ ] 모델 수정본을 검증·재패키징하고 최소 조작 재배포 절차를 안내한다.

## 2026-08-24 Groq 통합 및 죽은 코드 정리

- [x] Gemini 전용 미사용 파일(`server/vercelRadio.ts`)에 갇혀 있던 개선 프롬프트를 실제 배포 경로 `api/_lib/radio.ts`로 이관한다.
- [x] 대본 길이 가이드(title 5~20자, opening 40~150자, body 60~200자, closing 30~100자)를 프롬프트에 넣어 스키마 상한과 맞춘다.
- [x] 기계적인 대본을 200으로 반환하던 `generateLocalScript` 로컬 폴백을 제거하고 429/503을 실패로 알린다.
- [x] 클라이언트가 쓰지 않는 tRPC 방송 경로(`radio` 라우터, `server/radio.ts`, `server/elevenlabs.ts`)와 대응 테스트를 삭제한다.
- [x] `api/radio-api.test.ts`를 Groq 기준으로 재작성하고 프롬프트 회귀 테스트를 추가한다.
- [x] `tsconfig.json`이 `api/**`와 테스트 파일까지 타입 검사하도록 include/exclude를 고친다.
- [x] README·최종 점검표의 환경 변수를 `GROQ_API_KEY`·`GROQ_MODEL`·`GROQ_FALLBACK_MODEL`로 동기화한다.
- [x] BGM을 외부 CDN URL 대신 `client/public/audio` 정적 파일로 되돌린다.
- [x] ElevenLabs `voice_settings`에 voice id가 섞여 전송되던 문제를 고친다.
- [x] `.vercel/`을 `.gitignore`에 넣고 git 추적에서 제외한다.
- [ ] 제출자 본인이 GCP 콘솔에서 유출된 서비스 계정 키(`ais-gemini-key-fc3aa80cbc4f4cc@791137794941.iam.gserviceaccount.com`)를 폐기한다. 커밋 `6e9e25e`에 포함되어 공개 저장소 히스토리에 남아 있다.
- [x] ElevenLabs 모델을 `eleven_flash_v2_5`로 바꿔 문자당 요금을 절반으로 낮춘다.
- [x] 합성에 넘기는 대본 길이를 `MAX_SPEECH_CHARACTERS`(400자)로 제한해 1회 크레딧 상한을 고정한다.
- [x] 에피소드 id별 오디오 캐시를 도입해 즐겨찾기·재생 시 재합성이 일어나지 않게 한다.
- [x] `/api/generate`·`/api/synthesize`에 `Origin`/`Referer` 검사를 넣어 스크립트를 통한 크레딧 소모를 차단한다.
- [x] 유출됐던 GCP 서비스 계정 키를 폐기한다. (2026-08-24 사용자 조치 완료)
- [ ] 실제 `GROQ_API_KEY`로 4개 DJ 모드의 대본 품질과 길이 상한 초과 여부를 확인한다. (크레딧 절약을 위해 보류 중)
- [ ] 커스텀 도메인을 붙이면 `ALLOWED_ORIGIN_HOSTS`에 등록한다.

### 위 작업으로 무효가 된 이전 항목

Gemini 관련 미완료 항목(모델 404 확인, 지원 모델 ID 교체, Gemini 배포 코드 확인)은 Groq 전환으로 해당 없음.
